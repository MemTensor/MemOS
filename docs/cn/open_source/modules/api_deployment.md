# MemOS API

## 默认入口与部署

- 使用 **`server_api.py`** 作为 **公开开源版本** 的 API 服务入口。
- 可通过 **`docker/Dockerfile`** 进行部署。

以上是运行和部署 API 的默认通用方式。

## 扩展与参考实现

- **`server_api_ext.py`** 和 **`Dockerfile.krolik`** 是某位开发者扩展的 API 与部署方案，**仅供参考**。它们尚未与云服务集成，仍处于测试阶段。
- 如果你需要扩展或自定义行为，可以参考上述实现，并根据需要使用或调整。
