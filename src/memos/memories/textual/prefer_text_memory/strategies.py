from enum import Enum

class BuildStrategy(Enum):
    """Strategy for building memories."""
    NAIVE = "naive"

class RetrieveStrategy(Enum):
    """Strategy for retrieving memories."""
    NAIVE = "naive"

class UpdateStrategy(Enum):
    """Strategy for updating memories."""
    NAIVE = "naive"

class AssembleStrategy(Enum):
    """Strategy for assembling memories."""
    NAIVE = "naive"

